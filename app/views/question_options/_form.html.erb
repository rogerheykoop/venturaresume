<%= simple_form_for(@question_option) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :name %>
    <%= f.association :question %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
